{
  "name": "defaulters",
  "version": "1.0.0",
  "description": "Create an API using an AWS Lambda function that retrieves a list of defaulters",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "locally": "node -e \"console.log(require('./index').handler(require('./event.json')));\""
  },
  "author": "Divya Gupta",
  "license": "MIT",
  "dependencies": {
    "mongodb": "^5.7.0"
  }
}
